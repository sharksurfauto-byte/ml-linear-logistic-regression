# ml-linear-logistic-regression
This project implements and evaluates Linear and Logistic Regression using scikit-learn. Includes preprocessing, model training, metric analysis (RMSE, R², F1, ROC-AUC), and visual diagnostics. Serves as a reproducible baseline for classical ML experimentation.
